# 2026-07-03

## 9:20 A+H开盘前瞻报告
- 使用新脚本 `tools/ah_preopen_longport.py` (官方longport SDK)
- 原因：`longbridge_provider.py` 的REST API端点返回404，已废弃
- 美股数据：腾讯API（ETF映射）
- 数据获取：A股20/20 ✅，港股16/16 ✅，美股3/3 ✅
- 报告已保存: `~/.openclaw/workshop/data/ah_preopen_longport_2026-07-03.md`

## 关键发现
- 长桥官方SDK `longport.openapi` 可用，需使用 `Config.from_env()` + `QuoteContext`
- `SecurityQuote` 对象属性：last_done, prev_close, high, low, open, volume, turnover, timestamp
- `SecurityStaticInfo` 对象属性：name_cn, name_en, name_hk
- 涨跌需手动计算：`(last_done - prev_close) / prev_close * 100`
- `.longbridge.env` 中的环境变量通过 `venv_runner.sh` 正确加载（xargs会自动去掉引号）

## 教训
- `longbridge_provider.py` 自定义REST端点已过时，应优先使用官方 `longport` SDK
- 未来所有长桥相关脚本应使用 `longport.openapi` 而非 `longbridge_provider.py`

