# 分析报告错误总结与改进措施

## 📋 本次分析错误汇总

### 错误1: 使用了旧版搜索工具

**问题描述**:
- 先调用了 `comprehensive_stock_analyzer.py`（旧版分析脚本）
- 该脚本使用的是旧版知识星球获取方式
- 没有第一时间使用已部署的优化版 `multi_source_news_v2.py`

**正确做法**:
```python
# ✅ 应该直接使用优化版
from skills.dounai-investment-system.scripts.multi_source_news_v2 import (
    MultiSourceNewsSearcher, 
    ZsxqSearcher
)

# 使用优化版搜索知识星球数据
zsxq = ZsxqSearcher()
results = zsxq.search("关键词", count=15)
```

**改进措施**:
1. 分析前先检查是否有优化版工具可用
2. 直接使用 `multi_source_news_v2.py` 进行搜索
3. 不再调用旧版 `comprehensive_stock_analyzer.py` 作为主要数据源

---

### 错误2: 报告生成不连贯

**问题描述**:
- 分多次执行代码片段生成报告
- 导致报告格式不统一、内容有缺失
- 用户看到的是片段而非完整报告

**正确做法**:
- 一次性生成完整报告并保存为文件
- 最后统一输出给用户

**改进措施**:
```python
# ✅ 完整生成后一次性输出
report = generate_full_report(stock_code)
with open(f'analysis_{stock_code}_report.md', 'w') as f:
    f.write(report)
print(report)  # 最后统一输出
```

---

## ✅ 确保10模块完整的方法

### 标准10模块检查清单

每次分析必须包含以下10个模块：

```
✅ 0️⃣ 投资摘要
   - 股票代码、公司名称、行业
   - 核心财务数据（营收、净利）
   - 一句话总结

✅ 1️⃣ 公司基本画像
   - 1.1 基本信息表
   - 1.2 核心概念板块
   - 1.3 估值水平

✅ 2️⃣ 业务结构分析
   - 2.1 主营业务概述
   - 2.2 收入结构表
   - 2.3 核心竞争力评分

✅ 3️⃣ 产业链定位与竞争格局
   - 3.1 产业链定位图（文字/图形）
   - 3.2 竞争格局表
   - 3.3 竞争优势与劣势

✅ 4️⃣ 订单与产能分析
   - 4.1 订单情况
   - 4.2 产能情况
   - 4.3 交付能力

✅ 5️⃣ 财务深度分析
   - 5.1 年度业绩对比
   - 5.2 季度趋势
   - 5.3 财务健康度评估

✅ 6️⃣ 行业景气度验证
   - 6.1 行业驱动因素表
   - 6.2 最新行业动态（带引用）
   - 6.3 行业数据汇总

✅ 7️⃣ 客户与供应商分析
   - 7.1 客户结构表
   - 7.2 供应商结构表
   - 7.3 供应链风险

✅ 8️⃣ 业绩预测与估值
   - 8.1 三情景业绩预测
   - 8.2 估值测算
   - 8.3 估值评估
   - 8.4 可比公司估值

✅ 9️⃣ 风险提示
   - 9.1 风险分级表（🔴🟡🟢）
   - 9.2 风险应对策略

✅ 🔟 投资建议
   - 10.1 投资逻辑评分
   - 10.2 操作建议
   - 10.3 调仓规则
   - 10.4 跟踪指标
   - 10.5 综合判断
```

---

## 🔧 标准操作流程（SOP）

### 步骤1: 检查优化版工具

```python
import sys
sys.path.insert(0, '/root/.openclaw/workspace/skills/dounai-investment-system/scripts')

# 使用优化版
from multi_source_news_v2 import MultiSourceNewsSearcher, ZsxqSearcher
```

### 步骤2: 多源数据获取

```python
# 1. 知识星球搜索（优化版）
zsxq = ZsxqSearcher()
results = zsxq.search("股票名称", count=20)

# 2. 读取历史数据
raw_dir = Path('/root/.openclaw/workspace/data/zsxq/raw')
# 读取最近30天数据

# 3. 其他数据源（Exa、Tushare等）
```

### 步骤3: 生成完整10模块报告

```python
def generate_full_report(stock_code, stock_name):
    report_sections = []
    
    # 依次生成10个模块
    report_sections.append(generate_section_0_summary())
    report_sections.append(generate_section_1_company())
    report_sections.append(generate_section_2_business())
    report_sections.append(generate_section_3_industry_chain())
    report_sections.append(generate_section_4_order())
    report_sections.append(generate_section_5_financial())
    report_sections.append(generate_section_6_outlook())
    report_sections.append(generate_section_7_customer())
    report_sections.append(generate_section_8_valuation())
    report_sections.append(generate_section_9_risks())
    report_sections.append(generate_section_10_recommendation())
    
    return '\n\n'.join(report_sections)
```

### 步骤4: 保存并输出

```python
# 保存文件
report_file = f'analysis_{stock_code}_report.md'
with open(report_file, 'w', encoding='utf-8') as f:
    f.write(report)

# 输出给用户
print(report)
```

---

## 📝 关键要点提醒

### 1. 工具使用优先级

| 优先级 | 工具 | 说明 |
|:------:|:-----|:-----|
| 1 | `multi_source_news_v2.py` | ✅ 必须使用优化版 |
| 2 | `zsxq_fetcher_prod.py` | 爬虫脚本（非分析时） |
| 3 | `comprehensive_stock_analyzer.py` | 仅作为参考模板，不直接使用 |

### 2. 数据源优先级

| 优先级 | 数据源 | 用途 |
|:------:|:-------|:-----|
| P1 | 知识星球v2.0 | 调研纪要、产业数据 |
| P2 | Exa全网 | 最新新闻、公告 |
| P3 | Tushare | 财务数据 |
| P4 | 券商研报 | 深度分析 |

### 3. 报告质量标准

- ✅ 必须包含10个模块
- ✅ 必须使用优化版搜索
- ✅ 必须有知识星球数据
- ✅ 必须有多源交叉验证
- ✅ 必须保存为md文件
- ❌ 不能分段生成
- ❌ 不能使用旧版工具

---

## 🎯 后续执行检查清单

每次分析前自问：

- [ ] 是否使用了 `multi_source_news_v2.py`？
- [ ] 是否搜索了知识星球最新数据？
- [ ] 是否读取了历史数据（30天）？
- [ ] 报告是否包含10个完整模块？
- [ ] 是否保存为 `analysis_XXXXX_report.md`？
- [ ] 是否标注了数据来源（知识星球v2.0）？

---

*错误总结日期: 2026-03-02*  
*改进措施生效日期: 立即生效*
